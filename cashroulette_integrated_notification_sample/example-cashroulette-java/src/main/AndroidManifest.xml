<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.example_cashroulette_java">

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidSDKCashRouletteExample"
        android:usesCleartextTraffic="true"
        tools:targetApi="n">
        <!-- region { KEYS } -->
        <meta-data
            android:name="avatye_appid"
            android:value="98d4d4c35d594451b21f54718e2bc986" />

        <meta-data
            android:name="avatye_appsecret"
            android:value="c395dbe200ad4493ade96fb92c988fcf1c8df2d3687d49a9ab6f31f7c05e2bf4" />
        <!-- endregion -->

        <!-- region { service } -->
        <service android:name=".NotificationService"/>
        <!-- endregion -->

        <!-- region { activity } -->
        <activity
            android:name=".IntroActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat"/>
        <!-- endregion -->
    </application>
</manifest>
